# Proyecto-Supermercado-Admin-DB
Primero se crea el entorno:
python -m venv venv ,
Segundo se instala todo:
pip install -r requirements.txt ,
Tercero se corre el entorno:
.\env.ps1 ,
Y ya al final el servidor:
python .\api.py 
